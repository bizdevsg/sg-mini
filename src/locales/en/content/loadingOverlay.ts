import type { AppMessages } from "../../shared/messages";

export const enLoadingOverlay: AppMessages["loadingOverlay"] = {
    title: "Loading Page",
    description: "Preparing content and assets...",
  };
